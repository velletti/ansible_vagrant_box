---
- hosts: all
  become: yes
  become_method: sudo
  tasks:
    - name: Install git
      apt: pkg=git

  vars_files:
    - vars/all.yml
  roles:
     - server
     - vagrant_local
     - create-ssl-files
  #   - ansible-nginx
     - franklinkim.apache2
     - mysql
     - ansible-role-bower
  #   - ansible-role-php7
     - ansible-role-php5.3
     - ansible-role-daemonize
     - ansible-role-mailhog
     - composer
     # - typo3-cms8

# This installs an empty OXID Shop V6 Beta. Only used ONCE for an empty shop!
  #   - oxidshop
  #   - zz-final-task
  #   - app
